package components

css titleClass() {
    font-size: { "70px" };
    color: { "#FFFFFF" };
    font-width: { "bold" };
    matgin-bottom: { "12px" };
    matgin-top: { "0px" };
}

templ Title(title string) {
    <h1 class={titleClass()}>{title}</h1>
}